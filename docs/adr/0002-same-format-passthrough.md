# 代理只做同格式直通，不做跨协议转换

Agent 转发时 Anthropic 协议进 → Anthropic 兼容上游、OpenAI 协议进 → OpenAI 兼容上游，仅替换鉴权头、可选重写模型名并记账，不解析或改写消息体结构。跨格式转换（如让 Claude Code 使用纯 OpenAI 上游）被明确排除：流式事件、tool_use、thinking/reasoning 等字段的双向映射是需要持续跟进上游变化的维护深坑（cc-switch 复杂度暴涨的来源之一），而中转站普遍同时提供两种兼容端点，收益不成比例。转发层在实现上保持分层，未来若确有需要可挂转换器，但不为其预留排期。
