# 存储用单文件 SQLite，拒绝 Postgres + Redis

claude-code-hub 用 PostgreSQL + Redis 支撑多用户并发与分布式限流；switchAPI 是单用户系统，写入量级是个人请求流水，SQLite 单文件足以承载全部权威数据（含永久保留的用量明细）。"备份 = 快照一个文件"直接支撑快照轮转、导出与恢复的设计，部署也免除两个外部依赖容器。若未来出现多用户需求（已明确不做），此决策需整体重估。
